const mediaQuery = window.matchMedia('(max-width: 762px)');
const handleScroll = (event) => {

    if (window.scrollY > 50) {

        document.getElementById('header__navbar').classList.add('header__fixed');
        document.getElementById('brand').classList.remove('hidden');
        document.getElementById('auth').classList.remove('hidden');
        // document.getElementById('logo').classList.add('hidden');
        // add padding top to show content behind navbar
        navbar_height = document.querySelector('.navbar').offsetHeight;
        document.body.style.paddingTop = navbar_height + 'px';
    } else {
        if (event.matches) {
            document.getElementById('brand').classList.remove('hidden');
        } else {
            document.getElementById('brand').classList.add('hidden');
        }
        document.getElementById('header__navbar').classList.remove('header__fixed');
        document.getElementById('auth').classList.add('hidden');

        // document.getElementById('brand').classList.add('hidden');
        // document.getElementById('logo').classList.remove('hidden');
        // remove padding top from body
        document.body.style.paddingTop = '0';
    }
}



// Функция, которая будет вызываться при изменении размера окна
// const handleScreenResize = (event) => {
//     if (event.matches) {
//         // Если размер экрана соответствует медиа-запросу, добавляем класс или атрибут
//         document.getElementById('brand').classList.add('hidden');
//     } else {
//         // Если размер экрана не соответствует медиа-запросу, удаляем класс или атрибут
//         document.getElementById('myElemebrandnt').classList.remove('hidden');
//     }
// };

// Вызываем функцию handleScreenResize при загрузке страницы и при изменении размера окна

window.addEventListener('scroll', handleScroll);
mediaQuery.addEventListener(handleScreenResize);
handleScreenResize(mediaQuery);
// window.addEventListener('load', handleScreenResize);
// window.addEventListener('resize', handleScreenResize);



console.log(document.documentElement.clientWidth)